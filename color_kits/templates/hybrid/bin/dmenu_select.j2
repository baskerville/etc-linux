#! /bin/dash

fn='sans-serif:pixelsize=11'
nb='{{background}}'
nf='{{foreground}}'
sb='{{bg_jump}}'
sf='{{fg_highlight}}'

dmenu -i -h "$PANEL_HEIGHT" -fn "$fn" -nb "$nb" -nf "$nf" -sb "$sb" -sf "$sf"
