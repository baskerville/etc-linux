begin
  prog = mplayer
  button = KEY_PLAYPAUSE
  config = pause
end
begin
  prog = mplayer
  button = KEY_MENU
  config = osd_show_progression
  repeat = 1
end
begin
  prog = mplayer
  button = KEY_PREVIOUS
  config = seek -10
  repeat = 1
end
begin
  prog = mplayer
  button = KEY_NEXT
  config = seek +10
  repeat = 1
end
begin
  prog = mplayer
  button = KEY_VOLUMEDOWN
  config = volume -1
  repeat = 1
end
begin
  prog = mplayer
  button = KEY_VOLUMEUP
  config = volume +1
  repeat = 1
end
