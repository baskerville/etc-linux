#! /bin/dash

fn='sans-serif:pixelsize=11'
nb='{{background}}'
nf='{{foreground}}'
sb='{{bg_bump}}'
sf='{{fg_highlight}}'

dmenu_run -i -h "$PANEL_HEIGHT" -fn "$fn" -nb "$nb" -nf "$nf" -sb "$sb" -sf "$sf" -p '>' -q
