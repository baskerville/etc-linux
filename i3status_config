general {
    interval = 5
    output_format = "none"
}

order += "wireless wlan0"
order += "time"

wireless wlan0 {
    format_up = "S⇅ %essid"
    format_down = "S^fg(red)☹"
}

time {
    format = "◷ %a %H:%M"
}

disk "/" {
    format = "%free"
}

# vim: set ft=conf:
