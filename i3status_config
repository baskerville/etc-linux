general {
    interval = 5
    output_format = "none"
}

order += "wireless wlan0"
order += "time"

wireless wlan0 {
    format_up = "◉"
    format_down = "○"
}

time {
    format = "%A %H:%M"
}

disk "/" {
    format = "%free"
}

# vim: set ft=conf:
